import * as graph from './commands/graph'
import * as dot from './commands/dot'
import * as json from './commands/json'

export default [
  graph,
  dot,
  json,
]

export {
  graph,
  dot,
  json,
}
